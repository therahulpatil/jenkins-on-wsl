pipeline {
	agent any
	
	environment {
		DOCKER_USERNAME = "${DOCKER_USERNAME}"
		DOCKER_ACCESS_TOKEN = "${DOCKER_ACCESS_TOKEN}"
		GIT_REPO = "${GIT_REPO}"
	}

	stages {

		stage ('SCM') {
	
			steps {

				git '${GIT_REPO}'
			}
	
		}
	
	}

}
